// options.ts
export enum EPageSizeOptions {
    Small = 24,
    Medium = 48,
    Large = 72,
    ExtraLarge = 96
}

export enum ESortTypeOptions {
    ASC = 'asc ',
    DESC = 'desc',
}

export enum ESortByOptions {
    Title = 'title',
    Views = 'views',
    CreatedDate = 'createdDate',
}

export enum EViewTypeOptions {
    Grid = 'grid',
    Table = 'table'
}

export enum ERowSizeOptions {
    XL = 2,
    L = 3,
    M = 4,
    S = 5
}